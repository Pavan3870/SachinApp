 streamlit == 1.48.0 # currently pinned to this version
 groq
 python-dotenv
MyMy PDF translation is perfect, but my DOCX translation is losing its layout. I want the DOCX handler to follow the same logic as the PDF handler.
​Please refactor the doc_translation_worker.py or DOCX logic to:
​Preserve Structure: Instead of extracting all text into a single string, iterate through every Table, Row, and Cell object individually.
​Targeted Replacement: Only translate the .text property of specific Paragraph and Run objects. This ensures that table borders, cell widths, and background colors are anchored and remain unchanged, just like my PDF coordinate logic.
​Image Parity: If the DOCX contains an image, pass that image blob through the exact same Gemini OCR pipeline used for the PDF 'imaged narrative.' Re-insert the translated result into the same InlineShape anchor.
​Font Consistency: Apply the same Unicode font settings used in the PDF to the DOCX style objects to ensure Portuguese and Spanish characters don't break the document formatting.
​Provide a version of the DOCX translation function that maintains 1:1 layout fidelity by replacing content 'in-place' within the document's XML structure
